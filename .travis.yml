langauge: go

go:
    - 1.5
    - 1.6

before_install:
    - go get github.com/tools/godep

install:
    - godep restore

script: ginkgo -r -v

notifications:
    email:
        on_success: change
        on_failure: always
